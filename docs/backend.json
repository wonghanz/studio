
{
  "entities": {
    "Case": {
      "title": "Case",
      "description": "A mystery investigation case.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "difficulty": { "type": "string" },
        "summary": { "type": "string" },
        "category": { "type": "string" }
      },
      "required": ["id", "title", "difficulty"]
    },
    "CaseProgress": {
      "title": "Case Progress",
      "description": "User progress for a specific case.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "caseId": { "type": "string" },
        "unlockedMissions": { "type": "array", "items": { "type": "string" } },
        "currentMissionId": { "type": "string" },
        "completionStatus": { "type": "string", "enum": ["in-progress", "completed"] },
        "detectiveRank": { "type": "string" }
      }
    },
    "WritingAttempt": {
      "title": "Writing Attempt",
      "description": "A record of a user's writing practice attempt across all modes.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "mode": { "type": "string", "enum": ["standard", "mystery", "journey", "quest"] },
        "contentId": { "type": "string" },
        "level": { "type": "string" },
        "title": { "type": "string" },
        "userText": { "type": "string" },
        "bandScore": { "type": "string" },
        "taskFulfilmentScore": { "type": "number" },
        "languageOrganisationScore": { "type": "number" },
        "totalScore": { "type": "number" },
        "cefrLevel": { "type": "string" },
        "feedback": { "type": "string" },
        "strengths": { "type": "array", "items": { "type": "string" } },
        "weaknesses": { "type": "array", "items": { "type": "string" } },
        "improvementHints": { "type": "array", "items": { "type": "string" } },
        "modelAnswer": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "mode", "userText", "bandScore", "createdAt"]
    },
    "WritingStreak": {
      "title": "Writing Streak",
      "description": "Tracking global writing consistency across all modes.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "currentStreak": { "type": "number" },
        "longestStreak": { "type": "number" },
        "lastActivityDate": { "type": "string", "format": "date" }
      }
    },
    "UserBadge": {
      "title": "User Badge",
      "description": "Rewards earned by the user.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "badgeId": { "type": "string" },
        "earnedAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/cases/{caseId}": {
      "schema": "Case",
      "description": "Collection of all available mystery cases."
    },
    "/users/{userId}/caseProgress/{caseId}": {
      "schema": "CaseProgress",
      "description": "Tracking user progress for mystery cases."
    },
    "/users/{userId}/writingHistory/{attemptId}": {
      "schema": "WritingAttempt",
      "description": "Detailed history of all writing attempts."
    },
    "/users/{userId}/writingStreaks/main": {
      "schema": "WritingStreak",
      "description": "User's global unified writing streak data."
    },
    "/users/{userId}/writingBadges/{badgeId}": {
      "schema": "UserBadge",
      "description": "Badges earned by the user."
    }
  }
}
